export const GAME_WIDTH = 800;
export const GAME_HEIGHT = 600;// Default game width/height (dynamic scaling will override this)
export const SHIP_SPEED = 400; // Speed at which the ship(later our character) moves
